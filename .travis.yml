language: php

sudo: false

php:
  - 7.0
  - 7.1

before_script:
  - composer self-update
  - composer install --prefer-source

script:
  - vendor/bin/phpunit -c phpunit.xml $PHPUNIT_FLAGS

after_script:
  - php vendor/bin/coveralls -v
